this is the new default dataset for the cancergrid metadata registry - it contains all of the stuff needed to instantiate an MDR.

The defaults.xml replaces the current defaults.xml in the distribution...

Steve 